.container {
  width: 100%;
  height: 100vh;
  background: linear-gradient(180deg, #1a1a2e 0%, #0f0f1a 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.title {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 120rpx;
}

.title-text {
  font-size: 100rpx;
  font-weight: bold;
  color: #ffcc00;
  text-shadow: 0 0 20px #ffcc00, 0 4px 0 #cc9900;
  letter-spacing: 10rpx;
}

.subtitle {
  font-size: 32rpx;
  color: #888;
  margin-top: 20rpx;
  letter-spacing: 5rpx;
}

.menu {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 30rpx;
  z-index: 10;
}

.menu-item {
  width: 350rpx;
  height: 90rpx;
  background: linear-gradient(180deg, #4a4a6a 0%, #2a2a4a 100%);
  border: 3rpx solid #6a6a8a;
  border-radius: 15rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 36rpx;
  letter-spacing: 8rpx;
  box-shadow: 0 5rpx 15rpx rgba(0,0,0,0.5);
  transition: all 0.2s;
}

.menu-item:active {
  transform: scale(0.95);
  background: linear-gradient(180deg, #5a5a7a 0%, #3a3a5a 100%);
}

.tank-icon {
  position: absolute;
  bottom: 150rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 120rpx;
  height: 120rpx;
}

.tank-body {
  width: 80rpx;
  height: 80rpx;
  background: #4a7c4a;
  border: 4rpx solid #2a5c2a;
  border-radius: 10rpx;
  position: absolute;
  bottom: 10rpx;
  left: 50%;
  transform: translateX(-50%);
}

.tank-turret {
  width: 12rpx;
  height: 50rpx;
  background: #2a5c2a;
  position: absolute;
  bottom: 50rpx;
  left: 50%;
  transform: translateX(-50%);
  border-radius: 3rpx;
}

.help-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.8);
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 100;
}

.help-modal.show {
  display: flex;
}

.help-content {
  width: 600rpx;
  max-height: 80vh;
  background: linear-gradient(180deg, #2a2a4a 0%, #1a1a2e 100%);
  border: 3rpx solid #6a6a8a;
  border-radius: 20rpx;
  padding: 40rpx;
  overflow-y: auto;
}

.help-title {
  font-size: 40rpx;
  color: #ffcc00;
  text-align: center;
  margin-bottom: 30rpx;
  display: block;
}

.help-text {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.help-text text {
  font-size: 26rpx;
  color: #ccc;
  line-height: 1.6;
}

.help-close {
  width: 200rpx;
  height: 70rpx;
  background: linear-gradient(180deg, #4a4a6a 0%, #2a2a4a 100%);
  border: 2rpx solid #6a6a8a;
  border-radius: 10rpx;
  color: #fff;
  font-size: 30rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 30rpx auto 0;
}