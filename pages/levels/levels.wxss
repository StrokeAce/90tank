.container {
  width: 100vw;
  height: 100vh;
  background: linear-gradient(180deg, #1a1a2e 0%, #0f0f1a 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.main-layout {
  display: flex;
  flex-direction: row;
  width: 100%;
  height: 100%;
  padding: 30rpx 40rpx;
  box-sizing: border-box;
  gap: 40rpx;
}

.left-panel {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 25rpx;
  width: 40%;
}

.right-panel {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 60%;
}

.header-title {
  font-size: 44rpx;
  color: #ffcc00;
  font-weight: bold;
  letter-spacing: 5rpx;
}

.mode-section, .difficulty-section {
  width: 100%;
}

.section-title {
  font-size: 24rpx;
  color: #aaa;
  margin-bottom: 12rpx;
  display: block;
  text-align: center;
}

.mode-buttons, .difficulty-buttons {
  display: flex;
  gap: 15rpx;
  justify-content: center;
}

.mode-btn, .diff-btn {
  width: 160rpx;
  height: 65rpx;
  background: linear-gradient(180deg, #3a3a5a 0%, #2a2a4a 100%);
  border: 3rpx solid #5a5a7a;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #888;
  font-size: 26rpx;
  transition: all 0.2s;
}

.mode-btn.active, .diff-btn.active {
  background: linear-gradient(180deg, #5a5a8a 0%, #4a4a6a 100%);
  border-color: #8a8aaa;
  color: #fff;
  box-shadow: 0 0 15rpx rgba(100,100,150,0.5);
}

.diff-btn.diff-easy.active {
  background: linear-gradient(180deg, #4a8a4a 0%, #3a6a3a 100%);
  border-color: #6aaa6a;
}

.diff-btn.diff-medium.active {
  background: linear-gradient(180deg, #8a8a4a 0%, #6a6a3a 100%);
  border-color: #aaaa6a;
}

.diff-btn.diff-hard.active {
  background: linear-gradient(180deg, #8a4a4a 0%, #6a3a3a 100%);
  border-color: #aa6a6a;
}

.levels-section {
  width: 100%;
}

.levels-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  justify-content: center;
}

.level-btn {
  width: 100rpx;
  height: 100rpx;
  background: linear-gradient(180deg, #4a4a6a 0%, #3a3a5a 100%);
  border: 3rpx solid #6a6a8a;
  border-radius: 15rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
}

.level-btn:active:not(.locked) {
  transform: scale(0.95);
}

.level-btn.locked {
  background: linear-gradient(180deg, #2a2a3a 0%, #1a1a2a 100%);
  border-color: #4a4a5a;
}

.level-num {
  font-size: 40rpx;
  color: #fff;
  font-weight: bold;
}

.level-btn.locked .level-num {
  color: #555;
}

.level-lock {
  position: absolute;
  font-size: 24rpx;
}

.info-section {
  margin: 10rpx 0;
}

.info-text {
  font-size: 22rpx;
  color: #888;
}

.back-btn {
  width: 200rpx;
  height: 65rpx;
  background: linear-gradient(180deg, #4a4a6a 0%, #2a2a4a 100%);
  border: 3rpx solid #6a6a8a;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 26rpx;
}

.back-btn:active {
  transform: scale(0.95);
}
